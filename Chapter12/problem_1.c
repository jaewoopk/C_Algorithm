#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int n, m;

int main() {
    scanf("%d %d",&n, &m);
    int G[n + 1][n + 1];
    for (int i = 1; i < m + 1; i++) {
        
    }
    return 0;
}